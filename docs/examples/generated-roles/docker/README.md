# docker

Example Ansible role generated by ansible-craft for Docker installation and configuration.

## Description

This role installs Docker CE and Docker Compose, configures the Docker daemon, and manages Docker networks and volumes.

**Generated with:**
```bash
ansible-craft new role "docker container runtime with docker compose and custom daemon configuration"
```

## Requirements

- Target: Ubuntu 22.04 / Debian 12 / RHEL 8+
- Ansible: 2.14+
- Root privileges required

## Role Variables

See `defaults/main.yml` for all available variables.

### Key Variables

```yaml
docker_edition: ce  # ce or ee
docker_compose_version: "2.24.0"
docker_users: []    # Users to add to docker group
```

## Dependencies

None.

## Example Playbook

```yaml
- hosts: docker_hosts
  roles:
    - role: docker
      vars:
        docker_users:
          - deploy
        docker_daemon_options:
          log-driver: json-file
          log-opts:
            max-size: "10m"
            max-file: "3"
```

## File Structure

```
docker/
├── tasks/
│   └── main.yml
├── handlers/
│   └── main.yml
├── templates/
│   └── daemon.json.j2
├── defaults/
│   └── main.yml
├── vars/
│   └── main.yml
├── meta/
│   └── main.yml
└── README.md
```

## License

MIT

## Author

Generated by ansible-craft
