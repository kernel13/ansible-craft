# nginx-ssl

Example Ansible role generated by ansible-craft for Nginx with SSL.

## Description

This role installs and configures Nginx as a web server with SSL/TLS support using Let's Encrypt certificates.

**Generated with:**
```bash
ansible-craft new role "nginx web server with SSL certificates from Let's Encrypt and gzip compression"
```

## Requirements

- Target: Ubuntu 22.04 / Debian 12
- Ansible: 2.14+
- Root privileges required

## Role Variables

See `defaults/main.yml` for all available variables.

### Required Variables

```yaml
nginx_domains:
  - example.com
  - www.example.com

nginx_letsencrypt_email: admin@example.com
```

### Optional Variables

```yaml
nginx_ssl_enabled: true
nginx_gzip_enabled: true
nginx_worker_processes: auto
nginx_worker_connections: 1024
```

## Dependencies

None.

## Example Playbook

```yaml
- hosts: webservers
  roles:
    - role: nginx-ssl
      vars:
        nginx_domains:
          - mysite.com
        nginx_letsencrypt_email: admin@mysite.com
```

## File Structure

```
nginx-ssl/
├── tasks/
│   └── main.yml
├── handlers/
│   └── main.yml
├── templates/
│   ├── nginx.conf.j2
│   └── vhost.conf.j2
├── defaults/
│   └── main.yml
├── vars/
│   └── main.yml
├── meta/
│   └── main.yml
└── README.md
```

## License

MIT

## Author

Generated by ansible-craft
