# lamp-stack

Example Ansible playbook generated by ansible-craft for deploying a LAMP stack.

## Description

This playbook deploys a complete LAMP (Linux, Apache, MySQL, PHP) stack on Ubuntu/Debian servers.

**Generated with:**
```bash
ansible-craft new playbook "deploy LAMP stack on Ubuntu servers with MySQL and PHP 8.2"
```

## Requirements

- Target: Ubuntu 22.04 / Debian 12
- Ansible: 2.14+
- Root privileges required

## Inventory

### Example Inventory

```yaml
all:
  children:
    webservers:
      hosts:
        web1.example.com:
        web2.example.com:
    databases:
      hosts:
        db1.example.com:
```

## Variables

See `group_vars/all.yml` for common variables and group-specific files for role-specific settings.

## Usage

```bash
# Deploy full stack
ansible-playbook -i inventory playbook.yml

# Deploy only webservers
ansible-playbook -i inventory playbook.yml --limit webservers

# Check mode (dry run)
ansible-playbook -i inventory playbook.yml --check
```

## File Structure

```
lamp-stack/
├── playbook.yml
├── inventory.example
├── group_vars/
│   ├── all.yml
│   ├── webservers.yml
│   └── databases.yml
└── README.md
```

## License

MIT

## Author

Generated by ansible-craft
