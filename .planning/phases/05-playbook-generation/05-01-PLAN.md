---
phase: 05-playbook-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/generation/schemas/playbook-plan.ts
  - src/generation/playbook/structure.ts
  - src/generation/role/sanitize.ts
autonomous: true

must_haves:
  truths:
    - "Playbook plan schema exists and is valid for Anthropic structured outputs"
    - "Playbook project structure can be created with required directories"
    - "Playbook name can be inferred from natural language description"
  artifacts:
    - path: "src/generation/schemas/playbook-plan.ts"
      provides: "TypeScript interfaces and JSON schema for playbook plan"
      exports: ["PlaybookPlanPreview", "PLAYBOOK_PLAN_SCHEMA"]
    - path: "src/generation/playbook/structure.ts"
      provides: "Playbook project directory creation"
      exports: ["createPlaybookStructure", "PLAYBOOK_DIRECTORIES", "REQUIRED_PLAYBOOK_FILES"]
    - path: "src/generation/role/sanitize.ts"
      provides: "Extended with inferPlaybookName"
      exports: ["inferPlaybookName"]
  key_links:
    - from: "src/generation/schemas/playbook-plan.ts"
      to: "Anthropic structured outputs API"
      via: "JSON schema format"
      pattern: "output_format.*json_schema"
---

<objective>
Create the foundational types and project structure utilities for playbook generation.

Purpose: Establish the data structures (plan schema) and filesystem utilities (project structure) that all other playbook generation components will depend on.

Output: Three files providing playbook plan TypeScript interfaces, JSON schema for AI, project directory creation, and playbook name inference.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-playbook-generation/05-RESEARCH.md

# Existing patterns to follow
@src/generation/schemas/plan-preview.ts
@src/generation/role/structure.ts
@src/generation/role/sanitize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create playbook plan schema</name>
  <files>src/generation/schemas/playbook-plan.ts</files>
  <action>
Create new file with TypeScript interfaces and JSON schema for playbook plans.

Follow the pattern from `schemas/plan-preview.ts` but with playbook-specific structure.

**Interfaces to create:**
- `PlaybookPlanTask` - name, module, purpose, optional role_extraction_hint
- `PlaybookPlanPlay` - name, hosts, purpose, tasks array, handlers array, has_pre_tasks, has_post_tasks
- `PlaybookPlanVariable` - name, optional default, description
- `PlaybookPlanGroupVar` - group name, variables array
- `PlaybookPlanPreview` - playbook_name, description, plays array, group_vars array, inventory_groups array

**JSON Schema for structured outputs:**
- Mirror the TypeScript interfaces
- Use `additionalProperties: false` on all objects (per decision 04-01-03)
- All arrays and required fields properly marked

Export both interfaces and `PLAYBOOK_PLAN_SCHEMA` constant.

Reference 05-RESEARCH.md "Plan Preview Schema (Playbook)" section for exact field definitions.
  </action>
  <verify>
- File exists at src/generation/schemas/playbook-plan.ts
- `bun run typecheck` passes
- Schema structure matches research doc (plays with hosts/tasks, group_vars with variables)
  </verify>
  <done>
PlaybookPlanPreview interface and PLAYBOOK_PLAN_SCHEMA constant are exported and type-safe.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create playbook project structure utilities</name>
  <files>src/generation/playbook/structure.ts</files>
  <action>
Create new file for playbook project directory creation.

Follow the pattern from `role/structure.ts` but with playbook-specific directories.

**Constants:**
- `PLAYBOOK_DIRECTORIES` - readonly array with `['group_vars']`
- `REQUIRED_PLAYBOOK_FILES` - readonly array with `['playbook.yml', 'inventory.example', 'group_vars/all.yml', 'README.md']`

**Interface:**
- `PlaybookStructureOptions` - playbookName, outputDir, dryRun optional
- `PlaybookStructureResult` - playbookDir, createdDirs array

**Function:**
- `createPlaybookStructure(options)` - creates playbook directory and subdirectories
- Uses `mkdir` with `{ recursive: true }`
- Returns created paths for logging
- Respects dryRun option (creates nothing if true)

Import `mkdir` from `node:fs/promises` and `join` from `node:path`.
  </action>
  <verify>
- File exists at src/generation/playbook/structure.ts
- `bun run typecheck` passes
- Exports PLAYBOOK_DIRECTORIES, REQUIRED_PLAYBOOK_FILES, createPlaybookStructure
  </verify>
  <done>
Playbook project structure can be created with `createPlaybookStructure()` function.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add inferPlaybookName to sanitize.ts</name>
  <files>src/generation/role/sanitize.ts</files>
  <action>
Extend existing file with playbook name inference function.

**Add new function `inferPlaybookName(description: string): string`:**

1. Convert to lowercase
2. Strip common action words from start: deploy, provision, configure, setup, set up, install, create, manage, update, upgrade
   - Use regex similar to role: `new RegExp(\`^${action}\\s+\`, 'i')`
   - Only strip first matching action word
3. Remove filler words using existing `FILLER_WORDS` constant
4. Split on whitespace/commas, filter empty strings and words >20 chars
5. Take first 3 meaningful words
6. If no words remain, return 'playbook'
7. Join with hyphens and pass through existing `sanitizeRoleName()` for final cleanup

**Export** the new function alongside existing exports.

The function should handle descriptions like:
- "deploy LAMP stack" -> "lamp-stack"
- "configure web servers with nginx" -> "web-servers-nginx"
- "set up kubernetes cluster" -> "kubernetes-cluster"
  </action>
  <verify>
- `bun run typecheck` passes
- `inferPlaybookName('deploy LAMP stack')` returns reasonable name
- `inferPlaybookName('configure web servers')` strips action word
  </verify>
  <done>
`inferPlaybookName` function exported from sanitize.ts, producing valid playbook names from descriptions.
  </done>
</task>

</tasks>

<verification>
- [ ] `bun run typecheck` passes with no errors
- [ ] All three files exist and export expected symbols
- [ ] Schema follows additionalProperties: false pattern
- [ ] Structure module creates group_vars directory
- [ ] Name inference handles common playbook descriptions
</verification>

<success_criteria>
1. PlaybookPlanPreview interface defines plays, group_vars, inventory_groups structure
2. PLAYBOOK_PLAN_SCHEMA matches interface structure with required fields
3. createPlaybookStructure() creates playbook-name/group_vars/ directory
4. inferPlaybookName() strips action words and returns hyphenated lowercase name
</success_criteria>

<output>
After completion, create `.planning/phases/05-playbook-generation/05-01-SUMMARY.md`
</output>
