---
phase: 10-role-wizard
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/wizard/role-wizard.test.ts
autonomous: true

must_haves:
  truths:
    - "Unit tests verify prompt functions return correct types"
    - "Unit tests verify wizard orchestration calls prompts in order"
    - "Unit tests verify Ctrl+C (ExitPromptError) is thrown correctly"
    - "Unit tests verify validation rejects invalid input"
    - "All wizard tests pass"
  artifacts:
    - path: "src/wizard/role-wizard.test.ts"
      provides: "Unit tests for role wizard"
      min_lines: 100
  key_links:
    - from: "src/wizard/role-wizard.test.ts"
      to: "src/wizard/role-wizard.ts"
      via: "import runRoleWizard"
      pattern: "import.*runRoleWizard"
    - from: "src/wizard/role-wizard.test.ts"
      to: "src/wizard/prompts.ts"
      via: "mock prompt functions"
      pattern: "mock.*prompts"
---

<objective>
Create comprehensive unit tests for the role wizard

Purpose: Validate wizard behavior including prompt flow, validation, and error handling. Ensure Ctrl+C cancellation works correctly.

Output: Complete test suite in `role-wizard.test.ts` with all tests passing
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-role-wizard/10-CONTEXT.md
@.planning/phases/10-role-wizard/10-RESEARCH.md
@.planning/phases/10-role-wizard/10-01-SUMMARY.md
@src/wizard/types.ts
@src/wizard/types.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wizard unit tests</name>
  <files>src/wizard/role-wizard.test.ts</files>
  <action>
Create unit tests for the role wizard using Bun test runner.

Structure tests in describe blocks:

1. **describe('showStepHeader')**
   - Test it outputs correct step format (mock console.log)
   - Test percentage calculation (1/4 = 25%, 2/4 = 50%, etc.)

2. **describe('promptDirectories')**
   - Mock @inquirer/prompts checkbox
   - Test returns array of RoleStructureDirectory
   - Test tasks is always included in result
   - Test validation rejects empty array (via mock)

3. **describe('promptPlatforms')**
   - Mock @inquirer/prompts checkbox
   - Test returns array of RolePlatform
   - Test validation for empty selection
   - Test validation for Generic + specific platform conflict
   - Test Generic alone is valid

4. **describe('promptHandlers')**
   - Mock @inquirer/prompts checkbox
   - Test returns array of RoleHandler
   - Test empty array is valid (handlers optional)

5. **describe('runRoleWizard')**
   - Mock all prompt functions
   - Test complete flow returns validated RoleWizardContext
   - Test result is validated by Zod schema
   - Test ExitPromptError propagates (simulate Ctrl+C)

Use Bun's mock function: `import { mock, spyOn } from 'bun:test'`
Mock @inquirer/prompts with: `mock.module('@inquirer/prompts', () => ({...}))`

Follow existing test patterns from src/wizard/types.test.ts.
  </action>
  <verify>`bun test src/wizard/role-wizard.test.ts` passes all tests</verify>
  <done>role-wizard.test.ts has 10+ test cases covering all wizard functions</done>
</task>

<task type="auto">
  <name>Task 2: Verify ExitPromptError handling</name>
  <files>src/wizard/role-wizard.test.ts</files>
  <action>
Add specific tests for Ctrl+C cancellation handling.

1. Add test: "throws ExitPromptError when user cancels at step 1"
   - Mock promptDirectories to throw ExitPromptError
   - Verify runRoleWizard throws ExitPromptError
   - Verify no subsequent prompts are called

2. Add test: "throws ExitPromptError when user cancels at step 2"
   - Mock promptDirectories to succeed
   - Mock promptPlatforms to throw ExitPromptError
   - Verify runRoleWizard throws ExitPromptError

3. Add test: "throws ExitPromptError when user cancels at step 3"
   - Mock promptDirectories and promptPlatforms to succeed
   - Mock promptHandlers to throw ExitPromptError
   - Verify runRoleWizard throws ExitPromptError

This validates RWIZ-06: "User can exit wizard with Ctrl+C"

Import ExitPromptError from @inquirer/prompts for proper error type checking.
  </action>
  <verify>`bun test src/wizard/role-wizard.test.ts` passes including ExitPromptError tests</verify>
  <done>Tests verify Ctrl+C cancellation at each wizard step</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify</name>
  <files>src/wizard/role-wizard.test.ts</files>
  <action>
Run the complete test suite and fix any issues.

1. Run wizard tests: `bun test src/wizard/role-wizard.test.ts`
2. Run all wizard tests: `bun test src/wizard/`
3. Run full test suite: `bun test`

If any tests fail:
- Read the error message
- Fix the test or implementation
- Re-run until all pass

Ensure no regressions in existing tests.
  </action>
  <verify>`bun test` passes with 0 failures</verify>
  <done>All tests pass, wizard is ready for CLI integration in Phase 12</done>
</task>

</tasks>

<verification>
1. `bun test src/wizard/role-wizard.test.ts` passes
2. `bun test src/wizard/` passes (all wizard tests)
3. `bun test` passes (no regressions)
4. Test coverage: showStepHeader, promptDirectories, promptPlatforms, promptHandlers, runRoleWizard
5. Error handling: ExitPromptError tests for all steps
</verification>

<success_criteria>
- role-wizard.test.ts contains 10+ test cases
- All wizard functions are tested
- ExitPromptError handling is tested at each step
- Full test suite passes
- Wizard is ready for CLI integration (Phase 12)
</success_criteria>

<output>
After completion, create `.planning/phases/10-role-wizard/10-02-SUMMARY.md`
</output>
