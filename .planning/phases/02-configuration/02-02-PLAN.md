---
phase: 02-configuration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/api/validate-key.ts
  - src/config/errors.ts
  - src/cli/index.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "API key can be validated against Anthropic API without consuming tokens"
    - "Missing API key produces clear, multi-line guided error message"
    - "API key is always masked in output (sk-ant-***...***)"
    - "Help and version commands work without any config"
  artifacts:
    - path: "src/api/validate-key.ts"
      provides: "API key validation via token counting endpoint"
      exports: ["validateApiKey", "ValidationResult"]
    - path: "src/config/errors.ts"
      provides: "Config-related error display functions"
      exports: ["displayMissingApiKeyError", "maskApiKey"]
  key_links:
    - from: "src/api/validate-key.ts"
      to: "https://api.anthropic.com/v1/messages/count_tokens"
      via: "fetch POST request"
      pattern: "fetch.*count_tokens"
    - from: "src/config/errors.ts"
      to: "boxen"
      via: "styled error box"
      pattern: "boxen\\("
---

<objective>
Add API key validation and graceful error handling for missing/invalid keys.

Purpose: Ensure users get clear guidance when API key is missing or invalid, and validation happens without wasting API tokens.

Output: validateApiKey() function and displayMissingApiKeyError() with multi-line guided setup message.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-configuration/02-CONTEXT.md
@.planning/phases/02-configuration/02-RESEARCH.md

Existing patterns:
@src/cli/output.ts (displayError with boxen pattern)
@src/errors/cli-error.ts (CLIError class)

Prior plan artifacts:
@src/config/index.ts (loadConfig, Config)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API key validation and masking utilities</name>
  <files>
    - src/api/validate-key.ts
    - src/config/errors.ts
  </files>
  <action>
1. Create src/api/validate-key.ts:
   - Define ValidationResult type: { valid: boolean; error?: string }
   - Implement validateApiKey(apiKey: string): Promise<ValidationResult>
   - Use fetch to POST to https://api.anthropic.com/v1/messages/count_tokens
   - Headers: Content-Type application/json, anthropic-version 2023-06-01, x-api-key
   - Body: { model: "claude-3-5-haiku-20241022", messages: [{ role: "user", content: "test" }] }
   - Handle responses:
     - 200: return { valid: true }
     - 401: return { valid: false, error: "Invalid API key" }
     - 403: return { valid: false, error: "API key lacks required permissions" }
     - Other: parse error message from response body
   - Handle network errors with try/catch

2. Create src/config/errors.ts:
   - Implement maskApiKey(key: string): string
     - If key empty or <= 12 chars, return "***"
     - Otherwise show first 11 chars (sk-ant-api0) + "***...***" + last 4 chars
     - Example: "sk-ant-api03-abc...xyz" -> "sk-ant-api0***...***xyz1"

   - Implement displayMissingApiKeyError(): void
     - Use chalk and boxen matching existing output.ts patterns
     - Multi-line message explaining:
       - What's needed (Anthropic API key)
       - How to get one (link to console.anthropic.com)
       - Two options to configure (env var or config save command)
       - Link to docs (can use placeholder URL for now)
     - Box style: yellow border, "Configuration Required" title
     - Output to stderr
  </action>
  <verify>
    - `bun run lint` passes
    - TypeScript compiles: `bunx tsc --noEmit`
    - Test masking:
      `bun -e "import { maskApiKey } from './src/config/errors.ts'; console.log(maskApiKey('sk-ant-api03-abcdefghijklmnop'))"`
      Should show: sk-ant-api0***...***mnop
    - Test error display (visual check):
      `bun -e "import { displayMissingApiKeyError } from './src/config/errors.ts'; displayMissingApiKeyError()"`
      Should show styled yellow box with guidance
  </verify>
  <done>
    - validateApiKey() calls token counting endpoint
    - validateApiKey() returns ValidationResult with valid/error
    - maskApiKey() hides middle of API key
    - displayMissingApiKeyError() shows multi-line guided message in styled box
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate config into CLI with lazy loading</name>
  <files>
    - src/cli/index.ts
    - src/config/index.ts
  </files>
  <action>
1. Update src/config/index.ts to re-export from errors.ts:
   - Re-export maskApiKey, displayMissingApiKeyError

2. Create a requireApiKey() function in config/index.ts:
   - Load config via loadConfig()
   - If api.key is undefined/empty, call displayMissingApiKeyError() and return null
   - Otherwise return the config
   - This will be called by commands that need API key (Phase 3+)

3. For now, CLI index.ts does NOT need modification:
   - Config loading will be integrated when commands are added
   - Help/version already work without config (Phase 1)
   - The config module is ready for Phase 3 to import and use

4. Update config/index.ts exports:
   - Export requireApiKey for commands that need API key
   - Export validateApiKey for config save command
  </action>
  <verify>
    - `bun run lint` passes
    - TypeScript compiles: `bunx tsc --noEmit`
    - Help still works: `bun run dev -- --help` shows help (exit 0)
    - Version still works: `bun run dev -- --version` shows version (exit 0)
    - Test requireApiKey without key:
      `ANTHROPIC_API_KEY= bun -e "import { requireApiKey } from './src/config/index.ts'; const c = await requireApiKey(); console.log(c === null ? 'null (expected)' : 'unexpected')"`
      Should show the missing key error box and print "null (expected)"
    - Test requireApiKey with key:
      `ANTHROPIC_API_KEY=test-key bun -e "import { requireApiKey } from './src/config/index.ts'; const c = await requireApiKey(); console.log(c?.api.key)"`
      Should print: test-key
  </verify>
  <done>
    - requireApiKey() returns config or null with error display
    - validateApiKey exported from config module
    - Help and version commands still work without config
    - No changes needed to CLI index.ts at this phase
  </done>
</task>

</tasks>

<verification>
1. Run linting: `bun run lint`
2. Run type check: `bunx tsc --noEmit`
3. Test CLI still works:
   ```bash
   bun run dev -- --help
   bun run dev -- --version
   ```
4. Test missing key error display:
   ```bash
   ANTHROPIC_API_KEY= bun -e "
   import { requireApiKey } from './src/config/index.ts';
   await requireApiKey();
   "
   ```
   Should show styled yellow box with configuration guidance
5. Test API key validation (requires real key to fully test, but can verify function exists):
   ```bash
   bun -e "
   import { validateApiKey } from './src/config/index.ts';
   const result = await validateApiKey('invalid-key');
   console.log('Valid:', result.valid, 'Error:', result.error);
   "
   ```
   Should show: Valid: false Error: Invalid API key
</verification>

<success_criteria>
- validateApiKey() returns { valid: true } for valid keys, { valid: false, error: string } for invalid
- maskApiKey() masks API keys as sk-ant-api0***...***XXXX
- displayMissingApiKeyError() shows professional multi-line guidance
- requireApiKey() returns config when key present, null with error when missing
- Help and version commands work without any configuration
- All code passes lint and type checks
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration/02-02-SUMMARY.md`
</output>
